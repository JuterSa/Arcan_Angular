import { FilterArtefactPipe } from './filter-artefact.pipe';

describe('FilterArtefactPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArtefactPipe();
    expect(pipe).toBeTruthy();
  });
});
