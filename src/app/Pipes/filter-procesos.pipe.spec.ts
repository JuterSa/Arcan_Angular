import { FilterProcesosPipe } from './filter-procesos.pipe';

describe('FilterProcesosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProcesosPipe();
    expect(pipe).toBeTruthy();
  });
});
