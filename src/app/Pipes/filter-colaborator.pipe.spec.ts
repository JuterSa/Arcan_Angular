import { FilterColaboratorPipe } from './filter-colaborator.pipe';

describe('FilterColaboratorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColaboratorPipe();
    expect(pipe).toBeTruthy();
  });
});
